# Example repository instructions

- Runtime: Python 3.11 or newer, standard library only.
- Run tests from this directory with `python -m unittest discover -s tests -v`.
- Keep the public `Order` and `list_orders` contracts typed.
- Preserve input order and do not mutate the caller's collection.
